import * as vscode from 'vscode';

// Command handler implementation will be added in later tasks
export async function compareFrameworks() {
    // Placeholder for framework comparison command
    vscode.window.showInformationMessage('Framework comparison command registered!');
}