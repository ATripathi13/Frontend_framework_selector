import * as vscode from 'vscode';

export function activate(context: vscode.ExtensionContext) {
    console.log('Frontend Framework Selector extension is now active!');
    
    // Register commands will be implemented in later tasks
}

export function deactivate() {}